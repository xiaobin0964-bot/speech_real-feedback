# 演讲真实反馈系统 - 产品需求文档（PRD）

**版本**: v0.1  
**日期**: 2025-03-15  
**产品类型**: Web 应用（无自建后端）

---

## 一、产品概述

### 1.1 产品定位

演讲真实反馈系统是一款轻量级 Web 应用，用于在演讲/汇报场景下收集观众对演讲质量的实时反馈。演讲者创建会话后，观众通过扫码进入页面，在演讲过程中用 👍（好）和 🤔️（需改进）表达感受；演讲结束后自动生成包含时间轴分布的反馈报告。

### 1.2 目标用户

- **演讲者**：需要即时、可量化反馈的汇报人、讲师、答辩者等。
- **观众**：参与现场或远程观看、希望匿名表达感受的听众。

### 1.3 核心价值

- 无后端部署：使用 BaaS（如 Supabase）或纯前端方案，无需自建服务器。
- 实时反馈：观众在演讲过程中随时点击，数据实时汇总。
- 时间轴可视化：报告展示 👍/🤔️ 随时间的变化，便于发现「哪一段讲得好/不好」。

---

## 二、技术约束与架构假设

### 2.1 「无后端」的界定

- **不做**：自建 Node/Python/Go 等后端服务、自建数据库、自建 WebSocket 服务。
- **允许**：使用第三方 BaaS（如 Supabase、Firebase）提供实时数据库与鉴权，或使用纯前端 + 本地/局域网方案（如同一浏览器多标签、或 P2P/本地 WebSocket）。

### 2.2 推荐技术方案（选一）

| 方案 | 说明 | 适用场景 |
|------|------|----------|
| **Supabase** | 使用 Realtime + PostgreSQL，房间/会话存库，观众反馈写入表，演讲者订阅变更 | 多设备、多观众、需持久化与报告 |
| **Firebase** | Firestore + Realtime，类似能力 | 同上，团队已用 Firebase 时 |
| **纯前端 + 同屏** | 演讲者与观众在同一设备不同标签页，用 `localStorage`/BroadcastChannel 同步 | 演示、单机、无网络 |

PRD 以下功能描述以「使用 Supabase 等 BaaS」为默认假设，若选纯前端方案，需在实现时替换为本地同步逻辑。

---

## 三、功能需求

### 3.1 角色与入口

- **演讲者端**：同一 Web 应用的「创建/管理演讲」流程（填写信息 → 生成二维码 → 开始/结束）。
- **观众端**：通过扫描演讲者展示的二维码进入「反馈页」，仅能看到 👍/🤔️ 与必要提示，无需登录。

### 3.2 演讲者端流程

#### 3.2.1 创建演讲

- **演讲题目**（必填）：单行文本，用于报告标题和观众端展示。
- **自定义时长**（必填）：单位分钟，范围建议 1–120，用于：
  - 观众端显示倒计时/进度（可选）；
  - 报告时间轴的最大时间范围。
- **操作**：点击「开始演讲」后：
  - 在 BaaS 中创建一条「演讲会话」记录（含题目、时长、开始时间、状态：未开始）。
  - 生成并展示**房间唯一链接**，以及该链接对应的**二维码**，供观众扫码。

#### 3.2.2 等待观众与开始

- 演讲者界面展示：当前房间链接、二维码、已进入观众数（若 BaaS 支持）。
- 演讲者点击「开始」后：
  - 会话状态变为「进行中」，记录实际开始时间。
  - 观众端才展示 👍/🤔️ 按钮并开始可点击；此前观众端仅显示「等待演讲开始」等提示。

#### 3.2.3 演讲进行中

- 演讲者端可展示：
  - 实时 👍 总数、🤔️ 总数（从 BaaS 实时聚合或订阅）。
  - 可选：简单实时折线/柱状图（按时间聚合）。
- 演讲者点击「结束演讲」后：
  - 会话状态变为「已结束」；
  - 观众端按钮禁用或页面提示「演讲已结束」；
  - 演讲者端跳转或弹窗展示**反馈报告**。

#### 3.2.4 反馈报告

报告内容必须包含：

1. **基础信息**  
   - 演讲题目、设定时长、实际开始/结束时间。

2. **汇总数据**  
   - 👍 总次数、🤔️ 总次数；可选：总反馈人数（按去重观众 ID 或设备）。

3. **时间轴分布**  
   - 横轴：时间（按 1 分钟或自定义粒度聚合）。  
   - 纵轴或叠加：👍 数量、🤔️ 数量（柱状图或折线图）。  
   - 可同时显示两条曲线/两组柱子，便于对比「哪段时间好/哪段时间差」。

4. **导出/分享（可选）**  
   - 报告支持生成图片或 PDF、或分享链接（若数据存于 BaaS，可做只读报告页）。

### 3.3 观众端流程

#### 3.3.1 进入房间

- 观众扫码或打开链接，进入唯一「房间」页面。
- 若会话不存在或已结束：提示「链接无效或演讲已结束」。
- 若会话未开始：显示「等待演讲开始」及演讲题目（可选）。

#### 3.3.2 演讲进行中

- 页面展示：
  - 演讲题目、当前状态（进行中）。
  - **👍** 与 **🤔️** 两个大按钮，表意明确：👍 = 这段讲得好，🤔️ = 这段需要改进/没懂。
- 交互：
  - 观众可随时点击 👍 或 🤔️；每次点击记为一条反馈，带时间戳（与演讲开始时间的相对秒数或绝对时间）。
  - 可选：防误触（如 3 秒内同一按钮仅计一次）、或限制每人每分钟每类最多 N 次。
- 数据：通过 BaaS 写入「反馈」表（房间 ID、类型 👍/🤔️、时间戳、可选匿名用户 ID）。

#### 3.3.3 演讲结束后

- 按钮禁用或隐藏，页面提示「演讲已结束，感谢您的反馈」。

### 3.4 数据模型（概念级，便于 BaaS 建表）

- **sessions 表**  
  - id, title, duration_minutes, status(created|live|ended), started_at, ended_at, created_at

- **feedback 表**  
  - id, session_id, type(thumb_up|thinking), timestamp(相对或绝对), optional_visitor_id, created_at

- **报告**  
  - 由前端或 BaaS 的聚合查询根据 `sessions` + `feedback` 计算生成，无需单独表（若需持久化报告可再加 report 表）。

---

## 四、非功能需求

### 4.1 性能与体验

- 观众端点击 👍/🤔️ 后，延迟 < 2s 内在演讲者端可见（依赖 BaaS 与网络）。
- 报告生成在演讲结束后 5s 内可展示（数据量在千级反馈内）。

### 4.2 兼容与安全

- 支持现代浏览器（Chrome、Safari、Edge、Firefox 近期版本）；移动端扫码后浏览器内打开即可。
- 房间链接建议使用不可猜测的 ID（UUID）；不暴露敏感信息；BaaS 侧做好行级权限（仅房间创建者可写 session 状态，所有人可读 session、可写 feedback）。

### 4.3 无后端实现要点

- 使用 BaaS 的 Realtime 或轮询拉取反馈与状态变更，避免自建 WebSocket。
- 若采用「纯前端」方案，需在 PRD 实现说明中明确：同步方式（如 BroadcastChannel、localStorage 事件、或同一 Tab 内状态提升）。

---

## 五、页面与路由结构（建议）

```
/                     → 演讲者：创建演讲（题目 + 时长 + 开始演讲）
/session/:id          → 演讲者：房间页（二维码、开始/结束、实时统计、跳转报告）
/session/:id/report   → 演讲者：反馈报告页（汇总 + 时间轴分布）
/join/:id             → 观众：反馈页（👍/🤔️，仅此页）
```

所有页面 SPA 即可，无需服务端路由。

---

## 六、里程碑与优先级

| 阶段 | 内容 | 优先级 |
|------|------|--------|
| M1 | 演讲者创建演讲（题目、时长）、生成房间链接与二维码 | P0 |
| M2 | 观众通过链接/扫码进入，见「等待开始」与「进行中」状态 | P0 |
| M3 | 观众端 👍/🤔️ 点击写入 BaaS，演讲者端实时展示总数 | P0 |
| M4 | 演讲者结束演讲，生成报告（汇总 + 时间轴分布） | P0 |
| M5 | 报告导出/分享、防误触与限流、观众数展示 | P1 |
| M6 | 移动端适配与无障碍 | P1 |

---

## 七、成功指标

- 演讲者能在 1 分钟内完成「创建 → 展示二维码 → 开始」。
- 观众扫码后 3 秒内看到正确状态（等待中/进行中）并可使用 👍/🤔️。
- 报告准确反映所有反馈的汇总与时间轴分布，与原始数据一致。

---

## 八、附录：名词与交互说明

- **👍**：表示「这段讲得好 / 认可」。
- **🤔️**：表示「这段需要改进 / 没懂 / 有疑问」。
- **时间轴分布**：将反馈按「相对演讲开始的时间」聚合到固定时间格（如每分钟），分别统计 👍 与 🤔️ 数量并绘图。

---

*本文档为演讲真实反馈系统 Web 应用的 PRD，实现时可根据所选技术方案（Supabase / Firebase / 纯前端）做细化为技术设计文档（如接口约定、表结构、前端状态机）。*
